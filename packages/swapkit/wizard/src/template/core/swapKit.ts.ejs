import { getSwapKitClient } from "./client";
<%_ if (config.enableTokens) { _%>
import { AssetValue } from "@stormcloud266/<%= config.clientType === 'sdk' ? 'sdk' : 'core' %>";

AssetValue.loadStaticAssets()
<%_ } _%>

export const swapKit = getSwapKitClient();
